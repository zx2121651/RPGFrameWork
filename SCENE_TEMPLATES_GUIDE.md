# 如何使用模板场景开始新关卡

本指南将向您展示如何利用项目中的示例场景作为“模板”，快速地开始制作一个您自己的新关卡，而无需从零开始配置。

## 什么是模板场景？

模板场景是一个已经为您配置好了特定游戏类型所需基本元素的场景。使用模板的好处是：

- **节省时间**: 您不需要每次都手动设置摄像机、添加角色、配置控制器。
- **避免错误**: 模板确保了您的基础设置是正确的，可以直接运行。
- **快速开始**: 您可以立刻开始专注于设计关卡和编写剧情，而不是进行繁琐的初始配置。

本框架为您准备了2D和3D的模板场景，并提供了创建2.5D场景的详细步骤。

---

## 如何开始一个2D关卡

1.  在 **项目(Project)** 窗口中，导航到 `2DSample/Assets/Scenes/` 目录。
2.  找到 `SampleScene.unity` 文件。
3.  选中该文件，然后按下 `Ctrl + D` (在Mac上是 `Cmd + D`)，或者通过菜单栏 `Edit -> Duplicate` 来复制它。
4.  您会得到一个名为 `SampleScene 1.unity` 的新文件。
5.  将这个新文件重命名为您自己的关卡名，例如 `Level1_2D.unity`。
6.  双击打开这个新场景。现在您可以在一个已经配置好2D角色和摄像机的环境中开始设计您的关卡了。

---

## 如何开始一个3D关卡

步骤与2D完全相同，只是路径不同。

1.  在 **项目(Project)** 窗口中，导航到 `3DSample/Assets/Scenes/` 目录。
2.  找到 `SampleScene.unity` 文件。
3.  复制该文件，并将其重命名为您自己的关卡名，例如 `Level1_3D.unity`。
4.  双击打开新场景，即可在一个配置好的3D环境中开始工作。

---

## 如何开始一个2.5D关卡

对于2.5D，我们不提供一个直接的模板场景，而是提供一份详细的设置指南，让您可以根据自己的需求灵活搭建。

- 请打开我们之前为您创建的 **`2.5D_SETUP_GUIDE.md`** 文件。
- 按照其中的步骤，您可以从零开始创建一个功能完善的2.5D横版卷轴场景。这个过程本身就是一次模板化的设置。
