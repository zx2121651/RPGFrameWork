# RPG 游戏框架新手入门指南

欢迎使用本RPG游戏框架！

## 第一章：引言

### 这是什么？
这是一个基于 Unity 引擎的、为制作剧情向、解密类RPG游戏而设计的框架。它的核心目标是让**不熟悉编程的设计师和新手**也能够通过简单的可视化操作，快速地搭建游戏的核心内容。

您不需要编写复杂的代码，只需要在 Unity 编辑器中创建和配置一些我们称之为“资源”的文件，就可以实现丰富的游戏逻辑，例如：
- 角色对话
- 剧情事件
- 任务流程
- 道具系统
- 分支选项

### 本指南将教您什么？
这份指南将完全从一个新手的视角出发，一步一步地带您了解如何在 Unity 中使用本框架。读完这份指南后，您将能够独立制作一个包含“与NPC对话 -> 接受任务 -> 寻找道具 -> 完成任务”这样基本流程的简单游戏。

本指南包含以下章节：
- **第二章：Unity编辑器与核心资源入门** - 带您熟悉Unity的基本界面和框架的关键文件。
- **第三章：创建第一个事件 - 对话** - 教您如何创建一段简单的NPC对话。
- **第四章：实现互动 - 选项和条件** - 教您如何让玩家做出选择，并根据不同情况触发不同事件。
- **第五章：与游戏数据交互** - 教您如何创建游戏中的道具，并在事件中把它交给玩家。
- **第六章：整合：一个简单的任务** - （本章为总结，将前几章内容串联起来）

那么，让我们开始吧！

---

## 第二章：Unity编辑器与核心资源入门

在开始制作内容之前，我们首先需要熟悉一下Unity编辑器的主界面和本框架的一些核心资源。

### Unity 主界面

当您打开 `2DSample` 或 `3DSample` 场景后，您会看到类似下图的界面。我们只需要关注其中最重要的四个窗口：

1.  **层级 (Hierarchy) 窗口 (左侧)**: 这里列出了当前场景中所有的“游戏对象 (GameObject)”。游戏对象是构成游戏世界的基本单位，比如玩家、NPC、摄像机、灯光等。
2.  **场景 (Scene) 窗口 (中间)**: 这是您的可视化工作区。您可以在这里通过拖拽来摆放和调整游戏对象的位置。
3.  **项目 (Project) 窗口 (底部)**: 这里是您的“资源管理器”，包含了项目所有的资源文件，比如脚本、图片、音频、预制件(Prefab)和我们框架特有的各种数据资源。
4.  **检视 (Inspector) 窗口 (右侧)**: 当您在“层级”或“项目”窗口中选中任何一项时，这个窗口会显示其详细信息和可配置的属性。**这是我们之后最主要的工作区域**。

### 核心资源在哪里？

本框架的所有核心功能都是通过创建和配置 **ScriptableObject** 资源来实现的。您可以把它们理解为一种特殊的数据容器。这些资源都存放在 `Project` 窗口的特定文件夹中。

对于 `2DSample` 示例项目，最重要的资源位于：
- `Assets/Game/assets/share/`: 这个文件夹存放了大部分共享的游戏数据。
  - `Item List Define 1`: 这是您的 **道具数据库**。所有游戏中的道具都在这里定义。
  - `Quest List 1`: 这是您的 **任务数据库**。
  - `Skill List`: 这是您的 **技能数据库**。
  - `Audio List`, `Image List`, `Prefab List`: 这些是 **资源管理器**，用于注册您想在事件中使用的音频、图片等资源。
- `Assets/Game/assets/Garden prologue/`: 这个文件夹存放了特定场景的 **事件列表**。文件名如 `New Event List.asset` 的就是事件树资源，我们的剧情和互动逻辑都在这里编写。

在下一章，我们将学习如何创建一个新的事件列表并编写第一段对话。

---

## 第三章：创建第一个事件 - 对话

现在，我们来动手创建游戏中最常见的内容：一段NPC对话。

### 1. 创建事件列表资源

首先，我们需要一个地方来存放我们的事件。
1.  在 **项目 (Project)** 窗口中，导航到您想存放事件的文件夹，例如 `Assets/Game/assets/Garden prologue/`。
2.  在该文件夹的空白处点击鼠标右键，在弹出的菜单中选择 `Create -> Event -> New Event List`。
3.  这会创建一个名为 `New Event List.asset` 的文件。您可以将它重命名，比如 `MyFirstEvent.asset`。

### 2. 编辑事件树

现在，选中我们刚创建的 `MyFirstEvent.asset`。**检视 (Inspector)** 窗口会显示出本框架的核心工具：**事件树编辑器**。

![事件树编辑器](pic/13.png)

这个编辑器看起来可能有点复杂，但我们一步步来看：
- **左侧的树状结构**: 这是您的事件流程图。`Root` 是所有事件的根节点。
- **右侧的属性面板**: 当您选中左侧的任何一个节点时，这里会显示该节点的类型和可配置的参数。

### 3. 添加“显示文本”事件

我们的目标是显示一段对话。
1.  在左侧的树状图中，选中 `Root` 节点。
2.  在右侧的属性面板中，找到 **`Type` (类型)** 字段。这是一个下拉菜单，里面列出了所有可用的事件类型。
3.  从 `Type` 下拉菜单中，选择 **`text`**。
4.  选择后，您会看到下面出现了新的参数，其中最重要的是 **`Str1`**。
    - **`Str1`**: 在这里填入您想显示的对话内容。例如：“你好，冒险者！”
    - 您还可以设置 `Str2` (角色名) 和 `Str3` (角色头像图片名)等。

![配置文本事件](pic/1.png)
*(上图展示了一个更复杂的文本事件，包含了角色名和头像)*

### 4. 在场景中触发事件

事件制作好了，但它还不会自动播放。我们需要在场景中设置一个触发器。
1.  在 **层级 (Hierarchy)** 窗口中，右键点击，选择 `Create Empty` 来创建一个新的空游戏对象，可以命名为 `MyEventTrigger`。
2.  选中 `MyEventTrigger`，在 **检视 (Inspector)** 窗口中，点击 `Add Component` 按钮。
3.  搜索并添加 `Runt Event Trigger` 组件。
4.  在这个组件上，您会看到一个名为 `Events` 的列表。将我们刚才创建的 `MyFirstEvent.asset` 文件从 **项目** 窗口拖拽到这个列表的 `Element 0` 位置。
5.  确保 `How To Start` 设置为 `auto` 或 `playerTouch`，这样当游戏开始或玩家接触该物体时，事件就会被触发。

现在，运行游戏，您应该就能看到您写的第一句对话了！在下一章，我们将学习如何让对话变得可以互动。

---

## 第四章：实现互动 - 选项和条件

静态的对话是不够的，现在我们来为游戏加入互动性。

### 1. 使用 `ShowChoices` 创建分支

`ShowChoices` 是我们之前添加的功能，它可以让玩家在多个选项中做出选择，从而导向不同的剧情分支。

1.  在您的事件树中，选中一个您想添加选项的节点（例如，一段对话之后）。
2.  在该节点上点击右键，选择 `Add Child` 来创建一个子节点。
3.  选中这个新的子节点，在 **检视 (Inspector)** 窗口中，将其 `Type` 设置为 `ShowChoices`。
4.  现在，关键的一步来了：再次选中 `ShowChoices` 节点，**并为它创建若干个子节点**。每一个子节点都代表一个选项。
5.  选中代表选项的第一个子节点（例如，名为 `Child` 的节点），在右侧的 **`Str1`** 字段中输入选项的文本，例如“好的，我接受这个任务。”
6.  选中代表选项的第二个子节点，在 **`Str1`** 字段中输入另一个选项的文本，例如“抱歉，我现在没空。”
7.  现在，您可以在这两个选项节点下，分别创建后续的事件（例如，接受任务后的对话，或拒绝任务后的对话），形成两个独立的剧情分支。

当游戏运行到 `ShowChoices` 节点时，它会自动将它所有子节点的 `Str1` 内容作为选项显示给玩家。玩家选择后，事件流就会自动跳转到对应的子节点继续执行。

### 2. 使用 `If` 和全局变量实现条件逻辑

有时候，您希望一段事件只在满足特定条件时才发生。例如，只有当玩家拥有“钥匙”时，才能打开一扇门。

#### a. 创建全局变量

首先，我们需要一个变量来记录游戏的状态。
1.  在 **项目 (Project)** 窗口中，找到 `Assets/Game/assets/share/` 目录下的 `Switch List 1.asset`。这是一个全局开关（布尔型变量）数据库。
2.  选中它，在 **检视 (Inspector)** 窗口中，您可以添加新的开关并命名。例如，我们添加一个名为 `hasKey` 的开关，初始值设为 `false` (取消勾选)。

![全局变量列表](pic/8.png)

#### b. 在事件中改变变量

当玩家获得钥匙时，我们需要将 `hasKey` 开关打开。在一个事件节点中（例如，玩家调查一个宝箱后），将事件 `Type` 设置为 `changeGlobalSwith`，然后在参数中设置开关名为 `hasKey`，`Bool1` 勾选上（设为 `true`）。

#### c. 使用 `If` 进行条件判断

现在我们来设置“开门”的事件。
1.  在事件树中，将一个节点的 `Type` 设置为 `If`。
2.  `If` 事件本身不执行任何操作，它的作用是检查其子节点的触发条件。为其创建一个子节点。
3.  选中这个子节点，在 **检视 (Inspector)** 窗口底部，您会看到 **`Conditions` (条件)** 设置区域。
4.  在这里添加一个条件：`Type` 选择 `Switch`，`Name` 填写 `hasKey`，`Compare` 选择 `equal`，`Value` 填写 `True`。
5.  然后，在这个带条件的子节点下面，创建真正“开门成功”的事件（例如，播放开门动画、显示“门打开了”的文字等）。

![事件条件设置](pic/9.png)

这样设置后，只有当 `hasKey` 开关为 `True` 时，开门的事件才会发生。您还可以创建另一个平级的子节点，设置条件为 `hasKey` 等于 `False`，来触发“这扇门锁着”的对话。

通过组合 `ShowChoices` 和 `If`，您已经可以构建出非常复杂的互动逻辑了！

---

## 第五章：与游戏数据交互

RPG游戏的核心之一就是与各种游戏数据打交道，最常见的就是“道具”。现在，我们来学习如何创建并使用道具。

### 1. 创建一个新道具

1.  在 **项目 (Project)** 窗口中，导航到 `Assets/Game/assets/share/` 目录。
2.  找到并选中 `Item List Define 1.asset` 文件。这就是我们的道具数据库。
3.  在 **检视 (Inspector)** 窗口中，您会看到一个道具列表编辑器。

![道具列表编辑器](pic/3.png)

4.  点击 **`添加元素`** 按钮来创建一个新的道具栏位。
5.  展开新的栏位，填写道具的详细信息：
    - **`道具id`**: 道具的唯一ID，不能重复。例如：`item_key`。
    - **`道具类型`**: 从下拉菜单中选择一个类型，例如 `item`。
    - **`道具名`**: 道具在游戏中显示的名字，例如“一把生锈的钥匙”。
    - **`描述文字`**: 道具的描述信息。
    - **`图片`**: 可以从项目窗口拖拽一张图片到这里，作为道具的图标。
    - **`使用次数`**: -2代表无限次，-1代表不可使用，大于0代表具体的可使用次数。

![填写道具信息](pic/11.png)

### 2. 在事件中给予道具

创建好道具后，我们需要通过事件把它交给玩家。
1.  回到您之前创建的事件树 (`MyFirstEvent.asset`)。
2.  在一个节点下（例如，玩家调查宝箱后），创建一个新的事件节点。
3.  将该节点的 `Type` 设置为 **`changeItem`**。
4.  在参数中：
    - **`Str1`**: 填写您刚才创建的道具的ID，即 `item_key`。
    - **`Int1`**: 填写要给予的道具数量。正数代表增加，负数代表减少。我们这里填写 `1`。

当这个事件被触发时，系统就会在玩家的背包中增加一个“一把生锈的钥匙”。同时，屏幕上会自动弹出一个提示“一把生锈的钥匙 +1”。

至此，您已经掌握了本框架最核心的几个功能：显示对话、创建分支、使用条件、与游戏数据交互。在下一部分，我们会将这些知识串联起来。
