# 2.5D 示例场景设置指南

本指南将教您如何从零开始，搭建一个使用3D模型的2.5D横版卷轴游戏场景。

## 1. 创建新场景

1.  在Unity的菜单栏中，选择 `File -> New Scene` 来创建一个空场景。
2.  在弹出的窗口中选择 `Basic (Built-in)` 模板，然后点击 `Create`。
3.  立即保存场景：选择 `File -> Save As...`，导航到 `3DSample/Assets/Scenes` 目录，并将其命名为 `2_5D_Sample.unity`。

## 2. 搭建基本场景环境

我们需要一个地面和一个背景墙。

1.  **创建地面**:
    - 在 **层级(Hierarchy)** 窗口中，右键点击 -> `3D Object -> Plane`。
    - 选中新创建的 `Plane` 对象。
    - 在 **检视(Inspector)** 窗口中，将其 `Transform` 的 `Position` 设置为 `(0, 0, 0)`。
    - 将其 `Scale` 设置为 `(5, 1, 1)`，使其成为一个长条形的平台。

2.  **创建背景墙**:
    - 再次右键点击 -> `3D Object -> Cube`。
    - 选中新创建的 `Cube` 对象。
    - 将其 `Position` 设置为 `(0, 5, 1)`，这样它会位于平面的后方并向上抬高。
    - 将其 `Scale` 设置为 `(50, 10, 1)`，使其成为一面宽大的墙。

## 3. 放置角色

1.  在 **项目(Project)** 窗口中，导航到 `3DSample/Assets/Game/prefab/` 目录。
2.  找到一个角色预制件，例如 `player.prefab`。
3.  将 `player.prefab` 从项目窗口拖拽到 **场景(Scene)** 窗口中，放在地面 `Plane` 的上方。
4.  选中场景中的 `player` 对象，在 **检视(Inspector)** 窗口中，找到并移除它身上已有的移动脚本（例如 `basicMove3D` 或类似的脚本），通过点击组件右上角的三个点，然后选择 `Remove Component`。

## 4. 附加新的2.5D控制器

1.  仍然选中场景中的 `player` 对象。
2.  在 **检视(Inspector)** 窗口中，点击 `Add Component` 按钮。
3.  搜索 `CharacterController2_5D`，并将其添加到角色上。
4.  这个脚本会自动为角色添加一个 `Rigidbody` 组件。请确保 `Rigidbody` 的 `Use Gravity` 是勾选状态，并且在 `Constraints` 中冻结了 `Position Z` 和所有的 `Rotation`。

## 5. 调整摄像机

1.  在 **层级(Hierarchy)** 窗口中，选中 `Main Camera`。
2.  将其 `Transform` 的 `Position` 设置为一个合适的位置，例如 `(0, 5, -10)`，这样它就能从侧面看到整个场景。
3.  将其 `Rotation` 设置为 `(20, 0, 0)`，使其稍微向下俯视。
4.  确保 `Projection` 设置为 `Perspective`。

## 6. 确保EventSystem存在

为了让UI和输入系统正常工作，场景中必须有一个 `EventSystem`。
- 在 **层级(Hierarchy)** 窗口中检查是否存在一个名为 `EventSystem` 的对象。
- 如果没有，右键点击 -> `UI -> Event System` 来创建一个。

现在，点击Unity编辑器的 **播放(Play)** 按钮。您应该能够使用 `A` 和 `D` 键或左右方向键来控制角色在场景中左右移动了，一个基本的2.5D场景就搭建完成了！
